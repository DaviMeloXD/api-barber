{
    "version": 2,
    "builds": [
      {
        "src": "api/vercel_wsgi.py",
        "use": "@vercel/python"
      }
    ],
    "routes": [
      { "src": "/(.*)", "dest": "api/vercel_wsgi.py" }
    ]
}
